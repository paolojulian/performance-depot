export const ENVIROMENTS = {
  spaceID: process.env.NEXT_PUBLIC_SPACE_ID ?? '',
  deliveryToken: process.env.NEXT_PUBLIC_DELIVERY_API_TOKEN ?? '',
  previewToken: process.env.NEXT_PUBLIC_PREVIEW_API_TOKEN ?? ''
}